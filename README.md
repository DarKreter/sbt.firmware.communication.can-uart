## Basic usage

```
./CanUart.py --can_socket canIf --uart_interface uartIf --direction bidirectional
```
eg.
```
./CanUart.py --can_socket can0 --uart_interface /dev/ttyUSB0
./CanUart.py --can_socket vcan0 --uart_interface /dev/ttyAMA0
```
